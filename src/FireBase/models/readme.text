سؤال ممتاز جدًا ✅

لو هتستخدم Firebase Authentication للـ Login و Register، يبقى هتقسّم البيانات بين حاجتين:

Firebase Auth بيحتفظ دايمًا بالبيانات الأساسية الخاصة بالمستخدم زي الـ:

1
البريد الإلكتروني email

كلمة المرور password (مشفرة داخليًا من طرف فيربيز، مش محتاج تخزنها يدويًا)

معرف المستخدم uid (وده مهم جدًا تربطه ببقية بيانات المستخدم داخل Firestore)

2

Firestore (هتحط فيه باقي البيانات الإضافية زي النوع، الاسم، الصورة، العنوان… إلخ)